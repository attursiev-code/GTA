# Программа использует только стандартную библиотеку Python (tkinter, csv, json).
# Внешние зависимости не требуются.
#
# Для запуска на Linux может понадобиться системный пакет с Tk, например:
#   sudo apt install python3-tk
#
# Для разработки/тестирования дополнительно ничего не нужно — тесты используют unittest.
