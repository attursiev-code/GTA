# Базовый функционал (CSV/JSON, GUI) использует только стандартную
# библиотеку Python (tkinter, csv, json).
#
# Для запуска на Linux может понадобиться системный пакет с Tk, например:
#   sudo apt install python3-tk
#
# Импорт реальной базы данных (data_io.load_field_database) читает Excel
# (.xlsx) и требует openpyxl:
openpyxl>=3.1

# Для разработки/тестирования дополнительно ничего не нужно — тесты
# используют unittest.
